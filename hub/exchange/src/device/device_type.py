class DeviceType:

    def __init__(self, name, input = False, maker = '', version = '0.0.0'):
        self.name     = name
        self.input    = input
        self.maker    = maker
        self.version  = version
